# 外审 r2(codex-audit-tutorial-150-r2)处置(批 155,2026-09-03)

回件 VERDICT: FAIL——15 项中 11 已修、4 部分修;新发现 5 项;补充事实 3 条。逐条处置如下,全部采纳项已改入 `TUTORIAL.md` 与脚本。

## 4 项「部分修」

| # | 诊断 | 药方 | 处置 |
|---|---|---|---|
| 150-07 脚本写死本机目录 | 对 | 给根目录推导 | 采纳并**改脚本**:三份 shell 工具改为 `ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"`,给 Windows 程序的路径用 `cygpath -m`;`grep D:/test` 为零;`bash -n` 三份通过 |
| 150-08 备用解释器在 cd 前求值指向父目录 | 对 | 给第 5 行改法 | 采纳:改为先 cd 到 ROOT,再用 `$ROOT/venv/Scripts/python.exe`,并加缺参数即退出 |
| 150-13 坑 14 仍写肯定式机制 | 对 | 对 | 采纳:坑 14 改为「机制未核」 |
| 150-14 坑 10 仍写「参数无解」 | 对 | 对 | 采纳:改为「本次五组扫描都没解决」 |

## 5 项新发现

| # | 诊断 | 处置 |
|---|---|---|
| r2-01 说话人标签来源写成本地 KGO | 对(映射脚本读 ENX json,本地 KGO 只做场景 001 对齐) | 采纳:§4.3 改写,并加映射行口径说明(补充事实 3 一并) |
| r2-02 §4.11「时间轴待定」过期 | 对 | 采纳:改为官方 LRC 取法、轴核对结果、whisper 降为历史备选 |
| r2-03 §4.11 歌词层实现写成 overlay 方案 | 对 | 采纳:按 build_pv3 的 numpy alpha 混合改写 |
| r2-04 build_pv.py 引用已删立绘与写死 OP 路径 | 对 | 采纳:教程明写 build_pv.py 已废,不再修它(v2/v3 是现行) |
| r2-05 提取脚本无载体 | 对(当时是一次性内联代码) | 采纳:落盘 `tools/extract_images.py`,教程改指它 |

## 补充事实

1. build_pv3.py 用法注释写成 build_pv2 —— 已改。
2. 立绘编号的角色归属只有用户口述、无姓名标签 —— 教程已写「用户纠正」,保持。
3. 四个说话人行数是映射行口径 —— 已并入 §4.3。

## 「无法核实」4 条

属证据档位:RVC 实际推理、ffmpeg 三条核对、`cp` 覆盖、立绘姓名归属,本批不额外制造;其中 ffmpeg 在 codex 沙箱 PATH 里不存在是它的环境限制,不是教程错。
