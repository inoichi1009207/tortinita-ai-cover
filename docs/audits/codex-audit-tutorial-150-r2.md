VERDICT: FAIL — 15 项中 11 项已修、4 项部分修；150-07/08 仍不足以保证异机复跑，150-13/14 的旧断言仍在坑表复现，且 §4.3/§4.11 新增了数据来源与 PV 实现错误。数据源为本次通读的 `TUTORIAL.md`、r1、处置单及下列逐项引用的本地文件。

## 审计口径

- 本次逐行读取：`TUTORIAL.md`（327 行）、`codex-audit-tutorial-150-r1.md`、`codex-audit-tutorial-150-disposition.md`、三份音频 shell 工具、数据集报告、说话人映射脚本及 PV 脚本/歌词时间轴文件。
- Git Bash 实测：§4.6 与 §7.2 的代码块均通过 `bash -n`；子 shell 内 `$PWD` 为 Git Bash 路径，传给 Windows 原生程序时转换为盘符路径；`cp` 为 GNU coreutils。天花板：语法与参数转换通过，不代表 Applio/ffmpeg 已完整执行。
- 结案条件：逐项判完 15 项；检查指定新增段与命令；扫描四类敏感信息；只产本审计件。拿不准按“疑”报。

## 15 项复核

| 编号 | 档 | 依据行号 | 备注 |
|---|---|---|---|
| 150-01 | 已修 | `TUTORIAL.md:298,301-302`；处置单 `:7` | 首步已成为可执行的 `bash` 命令，且前文明确工作目录。 |
| 150-02 | 已修 | `TUTORIAL.md:301-305`；`tools/build_pitch_version.sh:9`；`tools/fly_235.sh:12`；处置单 `:8` | `cp inst_full_B.wav inst_full_down2.wav` 正好桥接两脚本文件名；Git Bash 下命令语法成立。 |
| 150-03 | 已修 | `TUTORIAL.md:139-148`；处置单 `:9` | 子 shell、解释器、输入/输出、权重与索引路径均补齐；`$PWD/..` 写法在本机 Git Bash 的路径转换成立。当前虚拟环境能否真正推理另列“无法核实”。 |
| 150-04 | 已修 | `TUTORIAL.md:154-155,168`；处置单 `:10` | 已明确该块只是 v23 基线，定稿还要跑 `fly_235.sh` 叠第二窗。 |
| 150-05 | 已修 | `TUTORIAL.md:157-169`；`tools/build_pitch_version.sh:11`；`tools/fly_235.sh:12`；处置单 `:11` | 已把伴奏支路 `aresample=44100` 标为教程的防御性补充，并说明脚本没有。 |
| 150-06 | 已修 | `TUTORIAL.md:171,306-314`；处置单 `:12` | 已删“与源比对”，明确比较加标签前后的两个 MP3，并给出两条完整 MD5 命令及时长命令。 |
| 150-07 | 部分修 | `TUTORIAL.md:4,148,298-305`；`tools/build_pitch_version.sh:6,8`；`tools/fly_235.sh:3`；处置单 `:13` | 教程承认脚本写死本机目录，但只说“改第 5–8 行”，没有给应替换成的根目录推导；附录仍直接让读者运行该脚本。首部“路径一律相对”也与被引用脚本的绝对路径事实并置，复跑者仍需自行猜改法。 |
| 150-08 | 部分修 | `TUTORIAL.md:148,298-302`；`tools/build_pitch_version.sh:5-6`；处置单 `:14` | “150-07/08”只藏在笼统括注中，未明说备用解释器 `../venv/...` 是在 `cd` 前求值、会指向父目录。当前 Git Bash 中 `python` 不在 PATH，备用路径又失效，权威入口会在耗时任务开始前直接中断；应给第 5 行的确切改法。 |
| 150-09 | 已修 | `TUTORIAL.md:105`；`dataset/DATASET-REPORT.md:11-12`；处置单 `:15` | 已改为 165 包、001–190 有洞、逐条读采样率及 8,096/507 分布。 |
| 150-10 | 已修 | `TUTORIAL.md:113`；`dataset/DATASET-REPORT.md:13`；处置单 `:16` | 已明确 186 是总条目：`Scene.tbl` 1 个 + KGO 185 个。 |
| 150-11 | 已修 | `TUTORIAL.md:200-213`；处置单 `:17` | 已把能量表证据限定为第二段，并明写第一段未按同一尺子复核；不再称两段均由该表坐实。 |
| 150-12 | 已修 | `TUTORIAL.md:57-58`；处置单 `:18` | 已保留本次跨语种现象，并把 contentvec/分离表示机制降为未做消融或对照的经验解释。 |
| 150-13 | 部分修 | `TUTORIAL.md:71,246`；处置单 `:19` | §2 已正确降档为“机制未核”，但坑 14 仍用肯定式“相位声码器抹谐波相位”当根因，等于在后文把同一断言升回已核实。 |
| 150-14 | 部分修 | `TUTORIAL.md:69,213,242`；`tools/window_variants.sh:12-16`；处置单 `:20` | §2/§4.10 已限定为本次五组未解决，但坑 10 仍写“参数无解，只能换输入”，原全称断言尚未清掉。 |
| 150-15 | 已修 | `TUTORIAL.md:264`；`tools/build_pitch_version.sh:10-11`；`tools/fly_235.sh:2,4,7-10`；处置单 `:21` | 已改成“写进脚本里”，并如实区分 `fly_235.sh` 头注与 `build_pitch_version.sh` 命令体。 |

## 修订引入或本轮新发现

[r2-01] `TUTORIAL.md:113-118` | 数据来源写错 | 摘句：“说话人标签来自游戏脚本里每句台词自带的 `[Name]` 前缀” | 实际映射脚本读取/下载的是 SymphonicRain-ENX 的反汇编 JSON，不读取刚解出的本地 `dataset/kgo/`；本地 2010 版只做过场景 001 交叉对齐。当前文字和总流程把“本地 KGO 解包”与“ENX JSON 映射”接成了不存在的直接链。 | 依据：`tools/build_voice_map.py:1-3,6,9,27-46`；`dataset/DATASET-REPORT.md:14-17`。 | 建议：改成“标签来自 ENX 反汇编 JSON 的 Text `[Name]`；与本地 2010 版仅场景 001 对齐”，并明确本地 KGO 解包在现有映射脚本中未被消费。

[r2-02] `TUTORIAL.md:226` | 状态过期 | 摘句：“时间轴来源待定” | 当前仓内已有带 `src: netease-official` 的逐行时间轴，且歌词版构建脚本已经读取它；“待定 / 可选 faster-whisper”不再是当前状态。 | 依据：`pv/lyrics_timed.json:1-16`；`pv/lyrics_official.lrc:8-13`；`tools/build_pv3.py:1,18,24-29`。 | 建议：写明实际采用的官方歌词时间轴、是否做过轴偏移/人工校准，并把 faster-whisper 降为历史备选。

[r2-03] `TUTORIAL.md:225-226` | 实现描述错误 | 摘句：“每句渲染一张透明 PNG，用 `overlay=enable=...` 叠上” | 现有歌词版不是“落 PNG + ffmpeg overlay”：`build_pv3.py` 在内存中用 PIL 生成 RGBA 数组，再逐帧用 numpy 做 alpha 混合并把 rawvideo 喂给 ffmpeg。教程把早期计划写成了现行实现。 | 依据：`tools/build_pv.py:1-2`；`tools/build_pv3.py:16-30,54-69`。 | 建议：按现行脚本改写；若仍要介绍 overlay 方案，明确标为未采用的早期设计。

[r2-04] `TUTORIAL.md:224-225` | PV 复跑阻断 | 摘句：“朵朵唱歌立绘 + 翅膀”“`tools/build_pv.py` …出 `draft_pv_nolyrics.mp4`” | 脚本虽然实现了翅膀参数，却在所有场景调用中传 `wings=None`；同时脚本仍写死游戏 OP 的本机绝对路径，并引用当前 `pv/assets/sprites/` 中缺席的 `srchr143.png`、`srchr149.png`。按教程定位到该脚本复跑会失败，且生成结果也不含所称翅膀。 | 依据：`tools/build_pv.py:1,7-16,18-25`；缺席检索面：`rg --files pv/assets/sprites`。 | 建议：要么补齐资产并实际传翅膀节点，要么删“+ 翅膀”；OP 路径改为参数或相对根目录，并在命令前检查输入。

[r2-05] `TUTORIAL.md:106` | 工具/路径陈述无载体 | 摘句：“全部用 `pv/` 下的 PIL 脚本按索引直接切出” | 本次在 `pv/` 与 `tools/` 的脚本清单中未观测到封包按索引切 PNG 的脚本；现有 `build_pv*.py` 只打开已经提取好的 PNG 做合成。无法据当前交付物按该句重做提取。 | 依据：`tools/build_pv.py:3-17`；检索面：`rg --files pv tools` 及对 `EVCG|EVBG|HyPack` 的脚本检索。 | 建议：保存并列出真实提取脚本；若当时是一次性代码且未留档，直说“提取脚本未保存”，不要给出不存在的路径。

## 敏感信息

- 绝对本机路径：未观测到。检索面为 `TUTORIAL.md:1-327`，模式为盘符加反斜杠或正斜杠。
- 邮箱：未观测到。检索面为 `TUTORIAL.md:1-327` 的邮箱格式。
- 口令、密码、token、API key、secret、credential：未观测到值。`TUTORIAL.md:4` 仅出现“本文不含任何凭据”的说明，不是凭据。

## 本批未覆盖

无。15 项、五个指定重点段、敏感信息与开放式新错均已审；以下“无法核实”是证据档位，不是漏审。

## 收尾条件对照

- 条件1：达成——15/15 项均已给四档结论、当前行号依据与备注。
- 条件2：达成——§4.6/§7.2 已做 Bash 语法与路径转换检查，§4.2/§4.3/§4.11 已逐段核对并登记 5 项新发现。
- 条件3：达成——教程 327 行已扫描盘符绝对路径、邮箱及口令/凭据类关键词，结果见“敏感信息”。
- 条件4：达成——只生成本审计件；已回读关键结构，`report-gate` 以 15/15 通过。该闸天花板：只检报告结构和完成性措辞，不检审计判断真伪。

## 我没问到的重要事实

1. `tools/build_pv3.py:4` 的用法注释仍让用户执行 `build_pv2.py`；照注释运行会得到无歌词 v2，而不是该文件实现的歌词版。应把文件名改为 `build_pv3.py`。
2. §4.2 新编号段的视觉分组本身一致：`sheet_torta_candidates.png` 中 010–019/122/139–143/149–151 是同一红裙扎发角色，`sheet_sprites_1.png`/`sheet_sprites_2.png` 中 090–097/127–130/147 是另一位带音符角色；但图片没有角色姓名标签，因此“哪位叫朵朵/芙铃”仍依赖用户纠正，见下节。
3. §4.3 的四个说话人行数与 `dataset/voice_map.csv` 机械计数一致：Torta 1,741、Phorni 1,594、Fal 1,513、Chris 771；但这些是映射行口径，不等于去重后的训练音频数。该口径差异已在 `dataset/DATASET-REPORT.md:23-26` 说明，教程最好顺手加“映射行口径”。

## 无法核实

1. 没有执行完整 RVC 推理：当前 `venv-applio` 启动器指向的基础解释器不可用，依据载体为 `venv-applio/pyvenv.cfg:1,4-5`；因此 150-03 只核到命令结构与 Git Bash 路径转换，不核模型实际出声。
2. 没有执行 §7.2 的 ffmpeg/ffprobe 三条核对：当前 PowerShell 与 Git Bash 均无法解析 `ffmpeg`/`ffprobe`，与 `TUTORIAL.md:23` 的“系统 PATH”自报不一致；因此两文件 MD5 与 218.86 s 本轮未重证。
3. 没有实际执行 `cp`：当前目录缺 `inst_full_B.wav`，而执行会覆盖既有 `inst_full_down2.wav`；本轮只核了 `cp` 可用、源/目标命名闭合及 Bash 语法，不为只读审计制造或覆盖音频。
4. 立绘图片能证“两组角色不同”，不能仅凭无姓名的联系表独立证明两组各自对应朵朵或芙铃；本轮对姓名归属判为“文档未记载/用户已纠正”，不把视觉印象升格成已核实身份。
