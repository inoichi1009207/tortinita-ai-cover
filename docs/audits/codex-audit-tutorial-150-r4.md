VERDICT: FAIL — 基于本地副本与本地 PCM 音频实测，v3b 数值成立，但歌词守卫的当前挂载不可运行、非 UTF-8 输出会误拦合法窗口，且空窗口可误放行。

数据源：本地副本 `TUTORIAL.md`、`tools/lyric_window_check.py`、`tools/fly_235.sh`、`tools/fly_107.sh`、`pv/lyrics_timed.json`、`leadB_fly235.wav`、`leadB_spliced.wav`；截至 2026-09-04。本审计仅写本文件，不改被审文件。

[r4-01] `TUTORIAL.md:15`、`tools/fly_235.sh:5-10`、`tools/fly_107.sh:9-10` | 已核实-成立 | “154.0–157.2 ← 69.32–72.52”“157.3–162.0”“1.000/−0.100” | 四个飞入端点与脚本常数完全一致；和声窗在两份 fly 脚本中均为 157.3–162.0。互相关数值不在脚本常数中，不能仅由脚本文本核实，但本地音频复算成立。157.2 s 也位于局部低能量区。 | `154.0/157.2/69.32/72.52 + 6.594 = 160.594/163.794/75.914/79.114`，逐项误差 0；对单声道 PCM16/40 kHz 的成品窗 157.3–162.0 按原轴加 6.594 后复算 Pearson 相关：新主唱对原第二副歌 `1.000000`，对第一副歌对应窗 `-0.099588`，四舍五入为 `−0.100`；10 ms RMS 扫描在 157.17–157.23 s 均处低值，局部最低约 157.23 s。 | 数值无需修改；若要让教程证据可复现，补一句互相关所用文件、轴换算和相关定义。

[r4-02] `TUTORIAL.md:15` | 已核实-成立 | “教训:飞入前先逐句比两段歌词,同旋律不等于同词。” | 表述没有过强：它限定在“飞入前”的核对动作，且本次反例直接证明旋律对应不能推出歌词相同；没有声称所有同旋律片段必然不同词。 | `pv/lyrics_timed.json` 中第一副歌后句为 `"I am yours, you are mine"`，第二副歌后句为 `"You and I'll be alright"`；旧长窗测试确实得到不同文本列表。 | 保留原句即可。

[r4-03] `tools/lyric_window_check.py:6-12` | 缺陷（假放行） | `if A != B ...; sys.exit(2)`，否则“逐句同词” | 对有歌词行的窗口，逐行文本比较能拦住本次“同旋律不同词”；但两个窗口都未命中歌词时 `[] == []`，会以 0 放行。脚本也未验证参数有限、起点小于终点或列表非空，因此反向窗、越界窗、`NaN` 等可能绕过字面保证。 | 强制 UTF-8 运行时：当前两组合法挂载均退出 0；旧错误长窗 `154.0–160.6 ← 69.32–75.94` 因末行不同退出 2；`300–301 ← 400–401` 输出两个空列表并退出 0。 | 在比较前验证四值均有限、`start < end`，并令任一歌词列表为空时退出 2；最好同时检查两窗命中行数及边界覆盖。

[r4-04] `tools/fly_235.sh:5-6`、`tools/fly_107.sh:6-7` | 缺陷（守卫与动作脱钩） | 检查器参数是一组字面量，实际剪接使用另一组 `S0/S1/D0/D1` 字面量 | 当前数值恰好一致，但检查参数不由实际剪接变量派生；后续只改剪接常数而漏改检查调用时，守卫仍可检查旧安全窗并放行新危险窗，不能持续按字面保证实际飞入的歌词一致。 | 两脚本均把检查调用写在变量赋值之前；检查器收到成品轴字面量，而 ffmpeg 使用加 6.594 后的另一组独立字面量。没有机器关系约束两组值。 | 单源定义成品轴窗口和 `6.594` 偏移，由同一组变量同时生成检查器参数与 ffmpeg 剪接参数；至少增加一致性断言。

[r4-05] `tools/fly_235.sh:5`、`tools/fly_107.sh:6` | 缺陷（路径/运行时） | `"$ROOT/venv/Scripts/python.exe" tools/lyric_window_check.py ...` | 当前工作区的该 venv 启动器指向已不存在的基础 Python，直接以 103 退出；因此两处挂载目前没有执行歌词逻辑，只靠 `|| exit 2` 失败关闭。路径引用本身有正确加引号，脚本内 `chdir` 也能抵抗调用目录变化。 | 直接运行 `clipboard/ai-cover/venv/Scripts/python.exe` 四组用例均报基础 Python 不存在并返回 103；项目 venv 文件确实存在，所以仅做存在性检查也抓不到该故障。 | 重建/修复项目 venv，并在 fly 脚本前置运行 `python -c` 类健康探针；不要把“启动器文件存在”当成解释器可用。

[r4-06] `tools/lyric_window_check.py:7,10-12` | 缺陷（编码导致合法窗误拦） | UTF-8 读 JSON，但输出含 `✅/❌` | 源文件及 JSON 的 UTF-8 读取无问题；问题在 stdout：Windows/Git Bash 的 GBK 输出环境不能编码 emoji。合法窗口在打印成功标记时异常退出 1，随后被 fly 脚本归一为 2，导致所有合法飞入被拦；不一致窗口也会在打印错误标记时先异常，丢失预期诊断。 | 用可用 Python 在默认 GBK stdout 下运行，两组合法用例、旧错误用例和空窗用例均因 emoji 得到退出 1；设置 `PYTHONIOENCODING=utf-8` 后分别得到 0、0、2、0。 | 去掉不可编码符号，或在程序/调用端明确配置 UTF-8 并采用可降级的输出错误策略（如 `backslashreplace`）；为 GBK/重定向 stdout 增回归用例。

[r4-07] `tools/fly_235.sh:3-5`、`tools/fly_107.sh:4-6` | 已核实-成立 | `set -e` 与 `... || exit 2` | 写法成立：Bash 的 `errexit` 不会在 OR-list 左侧抢先退出；左侧失败后会执行 `exit 2`。两脚本语法也通过 Bash 静态检查。 | `bash -c 'set -e; false || exit 2'` 实测退出 2；两文件 `bash -n` 均退出 0。天花板：这只证明控制流与语法，不证明外部解释器、ffmpeg 或输入文件可用。 | 无需因 `set -e` 改写；保留显式 `|| exit 2`。

[r4-08] 四个被审文本文件 | 已核实-成立 | 敏感信息检查 | 未观测到凭据、邮箱或绝对本机路径。 | 检索面：`TUTORIAL.md`、检查器及两份 fly 脚本；检查常见 key/token/secret/password/authorization/bearer 形态、邮箱及 Windows/Unix 用户目录绝对路径形态。仅有运行时相对/自解析路径。 | 无需修改；结论只覆盖上述四个文本文件，不外推到 venv 二进制或整个目录。

## 本批未覆盖

无。任务点名的增量范围均已审阅；证据边界另列于“无法核实”。

## 没问到的

[r4-09] `TUTORIAL.md:155,217,270,330` | 集成矛盾 | 后文仍写“154.0–160.6”“两个副歌是同词同旋律” | 新增 v3b 段已宣布旧长窗会唱错词，但教程后续操作说明、方法总结、常数说明和核对表仍把旧长窗当定稿，并明确称两副歌“同词”。读者顺着正文执行仍可能复现 v3 的硬伤；这使页首修正没有贯穿教程。 | 行 155 仍称定稿叠 `154.0–160.6 s` 和声窗；行 217 仍称“同词同旋律”并给旧飞入；行 270、330 仍登记 `154.0–160.6`。 | 同批同步这些引用为 v3b 的实际飞入窗 `154.0–157.2 ← 69.32–72.52` 与和声窗 `157.3–162.0`，并改掉“同词”断言；如需保留旧值，明确标为已废弃反例，不能继续写成定稿步骤。

## 无法核实

- `clipboard/` 被 `.gitignore:79` 忽略，四个被审文件没有可用的 `git diff`；因此“本批新增/改动”的边界只能采用任务明确点名的 v3b 段、检查器和两处挂载，不能从版本历史独立重建。
- 两份 fly 脚本的完整媒体流水线未执行：当前项目 venv 启动器失效，且本审计环境的 PATH 未提供 ffmpeg/ffprobe。上述互相关由直接读取现有 WAV 样本复算；未据此宣称整条生成链可重跑。
