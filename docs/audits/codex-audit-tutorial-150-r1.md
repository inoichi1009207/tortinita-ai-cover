VERDICT: FAIL — 两处会让全新复跑链直接中断（附录首步被注释、伴奏文件名断链），另有推理工作目录、最终混音配方和语音包事实错误；数据源为本次通读的 `TUTORIAL.md`、三份 shell 工具及指定旁证。

## 审计口径与通过项

- 本次逐行读取：`TUTORIAL.md`（315 行）、`tools/build_pitch_version.sh`、`tools/fly_235.sh`、`tools/window_variants.sh`、`pv/发布稿件.md`、`recon-symphonic-rain.md`、`dataset/DATASET-REPORT.md`。
- 常数对照通过：`6.594`、`84.66`、`65.5–67.7`、`154.0–160.6 ← 69.34–75.94`、`0.841 / 1.259`、limiter `0.98`、推理基线 `index=0.6 / protect=0.35 / rmvpe` 均与脚本一致。`2^(-2/12)` 与表内 `0.890899` 的舍入一致。
- 数量与内部算术通过：净训练桶 `1,739 / 138.3 分钟` 与数据集报告一致；音域表 A→B→C 各档按 −2 半音递减；`218.86 s` 在教程与发布稿件中一致，但本次未用音频文件独立测量。
- 敏感信息检索：在 `TUTORIAL.md` 全 315 行未观测到 `X:\` / `X:/`、邮箱、口令、密码、token、API key 或凭据值。检索面仅为教程正文；工具脚本的本机绝对路径另见 [150-07]。

## 发现

[150-01] `TUTORIAL.md:294` | 类型(命令错) | 原文摘句：`# 1. B 版整曲:tools/build_pitch_version.sh B -2` | 问题：代码块中整行以 `#` 开头，粘贴执行时第一步什么也不会做；而下一步依赖 B 版中间产物。 | 你的依据：`TUTORIAL.md:293-296`；`tools/build_pitch_version.sh:1-3` 明示该脚本才是整曲构建入口。 | 建议：改成可执行行 `bash tools/build_pitch_version.sh B -2`，说明须在 `clipboard/ai-cover/` 下运行。

[150-02] `TUTORIAL.md:182,294` | 类型(命令错) | 原文摘句：`tools/build_pitch_version.sh B -2` 后接 `bash tools/fly_235.sh` | 问题：即便把第一步取消注释，链条仍会断：构建脚本以标签命名伴奏，B 版写出 `inst_full_B.wav`；飞入脚本却固定读取 `inst_full_down2.wav`。全新目录里不会自动得到后者。 | 你的依据：`tools/build_pitch_version.sh:9,11`；`tools/fly_235.sh:12`。 | 建议：统一文件名；更稳妥的是让 `fly_235.sh` 接收伴奏路径参数，并在开头检查输入存在。

[150-03] `TUTORIAL.md:140-144` | 类型(命令错) | 原文摘句：`python core.py infer ... --pth-path applio/logs/...` | 问题：命令没有给工作目录和应使用的虚拟环境。从教程根目录运行时找不到 `core.py`；若先进入 `applio/`，两个 `applio/logs/...` 又会多套一层目录。它不能按字面复跑。 | 你的依据：`tools/build_pitch_version.sh:6-8` 的实际做法是先进入 `applio`，调用 `../venv-applio/Scripts/python.exe`，并显式给输入、输出、权重和索引路径。 | 建议：把 §4.6 改成与脚本同构的完整子 shell 命令，或只给 `bash tools/build_pitch_version.sh <标签> <半音>` 作为权威入口。

[150-04] `TUTORIAL.md:150-159,164` | 类型(命令错) | 原文摘句：`定稿混音(v23 起沿用)` | 问题：展示的滤镜只含 `65.5–67.7 s` memory 窗，没有定稿所称的第二处 `154.0–160.6 s` 和声复活。照该块运行只能得到基线混音，不是正文第 13、164、212-213 行定义的两窗定稿。 | 你的依据：`tools/fly_235.sh:7-12,18` 才把第二窗 `VOL_V/VOL_I` 与 memory 窗叠加到 FLYHARM。 | 建议：把标题改成“v23 基线混音”，并明确“定稿还必须跑 `fly_235.sh`”；或展示包含两窗自动化的最终滤镜。

[150-05] `TUTORIAL.md:156` | 类型(命令错) | 原文摘句：`[1:a]aresample=44100,volume=-2dB` | 问题：教程配方与两份实际定稿脚本不完全一致：脚本的伴奏支路没有 `aresample=44100`。这未必改变当前 44.1 kHz 输入的结果，但不满足“按实际写法逐项复跑”。 | 你的依据：`tools/build_pitch_version.sh:11`；`tools/fly_235.sh:12`。 | 建议：二选一统一；若保留教程写法，说明它是防御性补充而非定稿脚本原样抄录。

[150-06] `TUTORIAL.md:166,302` | 类型(命令错) | 原文摘句：`出 mp3 后用 -f md5 与源比对流是否一致`、`两文件 -map 0:a -f md5 相同` | 问题：两处都不是完整命令；第 166 行的“源”也未指明。混音并重新编码后的 MP3 不可能与输入 WAV/伴奏流哈希相同；只有第 302 行所暗示的“FLYHARM MP3 与仅加标签后的 final MP3”才应相同。 | 你的依据：`TUTORIAL.md:158` 明示混音后用 libmp3lame 重编码；`TUTORIAL.md:298-302` 明示最终一步是 `-c copy`；`pv/发布稿件.md:8` 指定了应比较的两个文件。 | 建议：写出对两个明确文件分别执行的完整 `ffmpeg -i ... -map 0:a:0 -f md5 -` 命令，删除含混的“与源比对”。

[150-07] `TUTORIAL.md:4,182,294` | 类型(命令错) | 原文摘句：`路径一律相对 clipboard/ai-cover/`、`一条命令` | 问题：教程正文没有泄露本机路径，但它指向的三个复跑脚本都把工作区写死为本机绝对路径，推理参数也嵌入固定盘符。换盘符、换目录或非 Git Bash 环境会失败，故教程声称的相对路径/可搬运性不成立。 | 你的依据：`tools/build_pitch_version.sh:6,8`；`tools/fly_235.sh:3`；`tools/window_variants.sh:3,7`。 | 建议：脚本用自身位置推导根目录；所有输入输出由该根目录拼接，不嵌入盘符。

[150-08] `TUTORIAL.md:182,294` | 类型(命令错) | 原文摘句：`tools/build_pitch_version.sh <标签> <半音>` | 问题：构建脚本在 `cd` 到项目根之前计算 pitch；其备用解释器写作 `../venv/Scripts/python.exe`。若按教程从 `clipboard/ai-cover/` 调用且 PATH 中没有 `python`，备用路径会指向父目录下的 `venv`，不是教程列出的本目录 `venv/`。拿不准按“疑”报。 | 你的依据：`tools/build_pitch_version.sh:5-6`；`TUTORIAL.md:289` 把 `venv/` 列在 `clipboard/ai-cover/` 内。 | 建议：先确定并进入脚本根目录，再调用 `$ROOT/venv/Scripts/python.exe`；同时对缺参数和解释器不存在给出立即失败的信息。

[150-09] `TUTORIAL.md:105` | 类型(数值不一致) | 原文摘句：`VOICE/SREV001–017.PAK`、`MS-ADPCM 单声道 44.1k` | 问题：包范围和采样率都写错。旁证记录的是 165 个 `SREV*.PAK`，编号 001–190 有洞；8,603 条中 8,096 条为 44.1 kHz，另有 507 条为 48 kHz。按 001–017 枚举会漏掉绝大部分语料，按全 44.1 kHz 假定会错读 507 条。 | 你的依据：`recon-symphonic-rain.md:17`；`dataset/DATASET-REPORT.md:11-12`。 | 建议：改为“枚举 `VOICE/SREV*.PAK`（165 包，001–190 有洞）；逐条读取采样率，44.1 kHz×8,096、48 kHz×507”。

[150-10] `TUTORIAL.md:113` | 类型(数值不一致) | 原文摘句：`SCRIPT.PAK 186 条全是 Cocotte 压缩 ... 解出 KGO 脚本` | 问题：186 是封包总条目数，不是 KGO 脚本数；其中包含 `Scene.tbl`，KGO 为 185 个。当前写法容易让复核者期待 186 个 KGO。 | 你的依据：`recon-symphonic-rain.md:20`；`dataset/DATASET-REPORT.md:13`。 | 建议：改成“186/186 解压成功：`Scene.tbl` 1 个 + KGO 185 个”。

[150-11] `TUTORIAL.md:195,199-206` | 类型(过强) | 原文摘句：`病根一致:原唱在这两段主唱掉到 −30 dB 以下` | 问题：表格只直接支撑第二副歌 154、155 秒低于 −30 dB。按表头减去 84.66 s，第一副歌列对应约 69.34–74.34 s，并不是第 195 行定义的 61–67 s 病灶窗；且列出的第一副歌主唱值均未低于 −30 dB。由此不能把“两段病根一致”写成已被这张表坐实。 | 你的依据：`TUTORIAL.md:195,199-206`。 | 建议：补一张覆盖 61–67 s 的原始主唱/和声逐秒表；补证前改成“第二段已由能量表坐实，第一段是否同根未核”。

[150-12] `TUTORIAL.md:58` | 类型(过强) | 原文摘句：`音色和语言内容在模型里是分开表示的(contentvec 负责内容,跟语种关系弱)` | 问题：判定为“过强”。本项目成品最多支撑“这次日语对白微调出的模型能处理本曲中的英语演唱”；指定旁证没有模型消融、跨语种对照或架构材料，不能据此证明“音色与语言分开表示”及 contentvec 与语种关系弱这一机制断言。 | 你的依据：`TUTORIAL.md:11-14,57-58` 只有项目结果和解释；指定的三份脚本与数据集报告未提供该机制的检验。 | 建议：改成经验性表述：“本次实测日语对白语料可处理这首歌的英语段；可能与内容特征具有一定跨语种迁移能力有关，机制未在本文核实。”

[150-13] `TUTORIAL.md:71,241` | 类型(过强) | 原文摘句：`相位声码器类移调...谐波之间的相位关系被抹平` | 问题：判定为“过强”。本文实测只支撑“B2 的人声经 rubberband 后听感空灵”；没有相位测量，也没有能排除瞬态处理、formant、分离残留等因素的实验，因而“抹平谐波相位”不是本文坐实的根因。 | 你的依据：`TUTORIAL.md:71,184,208,241`；`tools/build_pitch_version.sh:9` 只显示调用了 rubberband，不提供相位判据。 | 建议：保留现象与工艺决策，机制降为“可能与移调算法的相位/瞬态处理有关，本文未核”。

[150-14] `TUTORIAL.md:62,69,208,237` | 类型(过强) | 原文摘句：`这个问题不能靠参数解，只能换输入` | 问题：判定为“过强”。脚本和正文充分支撑“本次窗口扫过的五组设置没有解决”，也支撑本次多声部输入出现抖动；但五组实验不能证明所有参数组合、所有 F0 方法或所有多声部输入都无解。“正常单声部几十音分”也没有指定旁证中的输出表可核。 | 你的依据：`tools/window_variants.sh:2,12-16` 给出五组扫描；`TUTORIAL.md:69,208` 自报本次结果。 | 建议：改成“本次 rmvpe/crepe/fcpe 与 index/protect 五组扫描均未解决，最终改用换输入”；删除普遍性的“不能/只能”。

[150-15] `TUTORIAL.md:259` | 类型(其他) | 原文摘句：`切点常数...写进脚本头注` | 问题：该治理性陈述与实际载体不符。`fly_235.sh` 头注含第二窗、偏移和 84.66；但 `build_pitch_version.sh` 头注没有 6.594、memory 窗或 v20 切点，常数只埋在命令体；`window_variants.sh` 头注也没有所列四组常数。 | 你的依据：`tools/build_pitch_version.sh:1-3,10-11`；`tools/fly_235.sh:1-4`；`tools/window_variants.sh:1-3`。 | 建议：要么把“头注”改成“脚本内”，要么真的把时间轴口径和全部关键常数集中到每个相关脚本的头注/变量区。

## 本批未覆盖

无。用户指定的五个审计面均已覆盖；“无法核实”项是证据档位，不是漏审项目。

## 我没问到的重要事实

1. **附录不是一条从零开始的闭环。** 除 [150-01]/[150-02] 外，`fly_235.sh` 还默认已有 `leadB_spliced.wav`、`inst_full_down2.wav` 和输出目录环境；脚本没有前置输入检查。教程应明确“从现成中间件复跑”还是“从主唱干声复建”，两种入口不能混写。依据：`tools/fly_235.sh:3-5,12`；`TUTORIAL.md:291-296`。
2. **“定稿配方”有两层，当前叙述把它们压成一层。** `build_pitch_version.sh` 产的是 B 基线（v20 拼接 + memory 自动化），`fly_235.sh` 才叠加 2:34 飞入/和声窗。把基线与最终补丁分开命名，读者更不易拿错文件。依据：`tools/build_pitch_version.sh:3,10-11`；`tools/fly_235.sh:2,16-18`。
3. **资源提取节的错误会污染教程可信度，且与训练净口径无关。** `1,739 / 138.3` 是对的，不代表 §4.2 的“17 包、全 44.1 kHz”也对；两者应分别修。依据：`dataset/DATASET-REPORT.md:5-7,11-12`；`recon-symphonic-rain.md:17`。
4. **发布规则旁证本身明确未逐页取证。** 教程第 263 行写“本次未取页核实”与发布稿件的自我降档一致，不能把发布稿件第 28-37 行的平台选项当已核实规则。依据：`TUTORIAL.md:263`；`pv/发布稿件.md:3-4,28-37`。

## 你无法核实、需要作者自己确认的点

1. `218.86 s`、最终 MP3 与 FLYHARM 的音频流 MD5、两处接缝无时长变化：指定文本互相一致，但本次没有把音频文件纳入旁证读取，故未核。
2. 音域表的原始 pyin 输出（D4、A#3/F4/B4、A/B/C 三版）与“高 8 / 5 半音”：表内移调算术自洽，原始测量记录未在指定旁证中，故未核。
3. `160–180` 音分、正常单声部“几十音分”、参数扫描“读数差 <2 dB / 谐波比差 <0.05”、两窗色度相关 `0.81`、飞入后高 `2–4 dB`：脚本能证明测试被设计过，不能证明这些结果值，需作者给原始输出或台账。
4. “18 首都带演唱（−20～−25 dBFS）”：较早的 `recon-symphonic-rain.md:18,43` 与 `dataset/DATASET-REPORT.md:39` 仍记为待检测，可能只是旁证尚未回写；指定材料不足以判断教程的新结论是否真的完成，故未核。
5. `−20 dBFS`=真人声、`−45 dBFS` 以下=残留、`−85 dBFS`=空的阈值在全文内部一致，但它们依赖分离模型、归一化和测量窗口；若要作为可迁移教程规则，需作者限定为“本项目本批音轨的经验判据”。
6. Applio、torch、audio-separator、ffmpeg/rubberband 的具体版本与当前 CLI 参数兼容性，本次仅由教程自报，指定旁证没有环境锁文件或 `--help` 输出，故未核。
