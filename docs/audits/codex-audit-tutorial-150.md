# 外审任务:交付物 `clipboard/ai-cover/TUTORIAL.md`(批 150)

你是只读审计者。审 `D:/test/clipboard/ai-cover/TUTORIAL.md`(约 320 行,中文教程:从游戏语料训 RVC 声库到翻唱调整完毕)。
可读的旁证:`D:/test/clipboard/ai-cover/tools/*.sh`、`tools/*.py`、`tools/*.cs`、`pv/发布稿件.md`、`recon-symphonic-rain.md`、`dataset/DATASET-REPORT.md`。
**只产审计文件,不改任何文件。** 出处只认你本次真读到的文件行;没读到就写「未核」。

## 要审什么(按代价不对称排序)

1. **教程里的命令与常数能不能按字面复跑**:§4.6 推理命令、§4.7 混音配方、§7.2 复跑命令、§7.3 常数表,逐条对照 `tools/build_pitch_version.sh`、`tools/fly_235.sh`、`tools/window_variants.sh` 里的实际写法。参数名、数值、文件名有出入的**逐条列**。
   代价:教程里一个错参数会让照做的人白跑几小时,而误报只花我一分钟核对 ⇒ **拿不准按「疑」报**。
2. **内部自洽**:同一个数在不同节是否一致(时长 218.86、偏移 6.594 / 84.66、窗口 154.0–160.6 与 69.34–75.94、语料 1,739 句/138.3 分钟、音域表、dBFS 判据);§2 原理段有无与 §4/§5 实测矛盾的说法。
3. **原理段的说法是否过强**:§2 里「contentvec 负责内容,跟语种关系弱」「相位声码器抹谐波相位」「多声部 F0 乱飞不能靠参数解」——哪些是被本文实测支撑的,哪些只是常识性解释却写成了断言;对每条给「支撑充分 / 过强 / 未核」。
4. **不该出现的东西**:凭据、口令、绝对本机路径(`X:\` / `X:/`)、用户邮箱;有就列行号。
5. **你发现的、我没问到的重要事实**——单独一节,这一节最值钱。

## 输出格式(写到 `D:/test/clipboard/ai-cover/codex-audit-tutorial-150-r1.md`)

- 首行:`VERDICT: PASS | PASS-WITH-FIXES | FAIL`,一句话理由。
- 每条发现:`[编号] 位置(行号) | 类型(命令错/数值不一致/过强/敏感信息/其他) | 原文摘句 | 问题 | 你的依据(文件+行)| 建议`。
- 结尾两节:「我没问到的重要事实」「你无法核实、需要作者自己确认的点」。
