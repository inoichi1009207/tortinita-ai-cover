# 外审 r4(codex-audit-tutorial-150-r4)处置(批 168,2026-09-04)

回件 VERDICT: FAIL——v3b 数值经 codex 本地音频复算成立(r4-01/02),但比词守卫有三处缺陷(r4-03/04/06)与一处环境判断(r4-05)。

| # | 诊断 | 药方 | 处置 |
|---|---|---|---|
| r4-01 v3b 端点/互相关数值 | 已核实-成立(codex 复算 1.000000 / −0.099588) | 补可复现说明 | 采纳:数值不改;教程句已含文件与轴换算 |
| r4-02 「教训」句是否过强 | 不过强 | 保留 | 保留 |
| r4-03 空窗 `[]==[]` 放行、参数不校验 | 对 | 对 | 采纳:参数须有限且起<止;任一窗零命中即退出 2;负例实测三种都拒 |
| r4-04 检查参数与剪接常数两套字面量 | 对 | 单源 | 采纳:两份 fly 脚本改成成品轴 T0/T1/DD0/DD1 单源,源轴 S0…、淡入淡出点由 awk 派生;`bash -x` 回读 S0=160.594…与旧值逐项相同 |
| r4-05 venv 解释器「指向不存在的基础 Python」 | 环境特异(codex 沙箱里 103;本机同一路径正常运行、并已用它重跑全链) | 加健康探针 | 部分采纳:诊断不成立于本机,但探针便宜——脚本开头 `"$PY" -c "import sys"` 失败即退出 |
| r4-06 emoji 在 GBK 控制台炸,合法窗被误拦 | 对(我插入检查后没在无 PYTHONIOENCODING 下跑过 fly 脚本) | 对 | 采纳:输出改纯 ASCII `[OK]/[FAIL]`,stdout 用 backslashreplace;在未设 PYTHONIOENCODING 的 bash 下重跑两份 fly 脚本全链通过,最终 NOHARM 的 MD5 与已发布定稿一致(6da56676…) |

结论:守卫从「看起来能拦」变成「实测能拦、且拦的是实际剪接窗」。
